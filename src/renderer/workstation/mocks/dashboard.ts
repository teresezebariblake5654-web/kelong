import type { DashboardMetrics } from '@workstation/types';

export const mockDashboardMetrics: DashboardMetrics = {
  monthFileCount: 18,
  completedTaskCount: 24,
  minutesSaved: 288,
  creditsConsumed: 140,
};
