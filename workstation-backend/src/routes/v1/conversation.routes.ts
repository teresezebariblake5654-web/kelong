import { Router } from 'express'; import { conversationController as c } from '../../controllers/conversation.controller'; import { requireAuth,requireOrganization } from '../../middleware/auth.middleware'; const r=Router(); r.use(requireAuth,requireOrganization); r.get('/',c.list);r.post('/',c.create);r.post('/import',c.import);r.get('/:id/messages',c.messages);r.patch('/:id',c.update);r.delete('/:id',c.remove);export default r;
