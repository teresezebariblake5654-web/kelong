{
  "id": "ask-user-question",
  "name": "AskUserQuestion",
  "description": "Structured user confirmation tool for Workhorse AI desktop.",
  "contracts": {
    "tools": [
      "AskUserQuestion"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "callbackUrl": {
        "type": "string",
        "description": "Workhorse AI HTTP callback URL for approval requests"
      },
      "secret": {
        "type": "string",
        "description": "Shared secret for callback authentication"
      }
    }
  }
}
